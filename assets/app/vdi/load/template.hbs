
<div class="randomize rocket centered fixed noselect">
</div>
{{#unless showVdi}}
  <div class="vdi-load-background fixed noselect">
    <div>
      <h2 class='text'>Loading</h2>
      <div class='white '>
        Please wait, we're trying to open a session.
        <p>
          Status:
          {{#if isUpdatingAppStatus}}
            Initializing..
          {{else}}
            {{display-guac-status remoteSession.loadState}}
          {{/if}}<br/>
          Waiting for plaza: {{plazaStatus}}<br/>
          <div class='abs'>
            {{#if vdiIsLoadingAndNoError}}
              Starting..
            {{/if}}
          </div>
        </p>
      </div>
    </div>
  </div>
{{else}}
  {{#if guacError}} 
    <div class="vdi-load-background centered">
      <div>
        <h2>An error occured.</h2>
        <div class='white '>
          Connection has failed.
          <p>
            Message: {{vdiLoadErrorMessage}}
          </p>
          <div>
            {{#icon-component action=(action 'retryConnection' remoteSessionData.retryConnection) materialIcon="refresh" hover-lighter=true class='clickable color-primary va-sup'}}
              <span class='color-primary va-sup'>
                Retry connection 
              </span>
            {{/icon-component}}
          </div>
          <div>
            {{#icon-component action='goToApp' materialIcon="replay" hover-lighter=true class='clickable color-primary va-sup'}}
              <span class='color-primary va-sup'>
                Go back
              </span>
            {{/icon-component}}
          </div>
        </div>
      </div>
    </div>
  {{/if}}
{{/unless}}
